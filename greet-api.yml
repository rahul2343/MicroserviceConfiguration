#server:
#  port: 8081
spring:
  application:
    name: GREET-API
  boot:
    admin:
      client:
        url: http://localhost:1111/
  config:
    import: optional:configserver:http://localhost:4444
        

management:
  endpoints:
    web:
      exposure:
        include: '*'
